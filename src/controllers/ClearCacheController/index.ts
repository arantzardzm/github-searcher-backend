import { Request, Response } from 'express';
// import axios from 'axios';

export default async function clearCacheController(req: Request, res: Response) {
  // try {
  //   const response = await isEligible(req);
  //   if (!response || !response.data) {
  //     return createFailResponse(req, res, 400, 'IsEligibleController: Error checking isEligible response', response);
  //   }
  //   return createSuccessResponse(res, 200, response.data);
  // } catch (err) {
  //   return createErrorResponse(req, res, 500, 'IsEligibleController: Error fetching isEligible response', err);
  // }
}
